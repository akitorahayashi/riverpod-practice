import 'package:flutter/material.dart';

class StructureBlock extends StatelessWidget {
  const StructureBlock({super.key});

  @override
  Widget build(BuildContext context) {
    return Column();
  }
}
