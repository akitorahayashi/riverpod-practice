import 'package:flutter/material.dart';

class EmployeesBlock extends StatelessWidget {
  const EmployeesBlock({super.key});

  @override
  Widget build(BuildContext context) {
    return const Column(children: [
      Text('EmployeesBlock'),
    ]);
  }
}
